services:
  backend:
    build:
      context: .
    container_name: trotamundos_api
    ports:
      - "3000:80"
    environment:
      RAILS_ENV: production
      RAILS_MASTER_KEY: cddd36811d9cccd6ed189edb503cd4d3
      DB_HOST: 67.205.148.136
      DB_PORT: 1433
      DB_USERNAME: sa
      DB_PASSWORD: Trota01@
      DB_NAME: Trotamundosdb
    networks:
      - trotamundos

networks:
  trotamundos:
    external: true
