#!/bin/bash
set -e

echo "PATH is: $PATH"
which bundle
which rails || echo "rails not found"

echo "Ejecutando migraciones de base de datos..."
bundle exec rails db:migrate || echo "No se pudieron ejecutar migraciones"

exec "$@"
